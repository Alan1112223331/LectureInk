# 听课稿 · LectureInk

Windows 原生桌面听课翻译工具。支持系统声音和麦克风，将选定来源的人声音频直接发送给硅基流动的 `Qwen/Qwen3-Omni-30B-A3B-Instruct`，由同一个音频模型生成原文和中文译文。本地只做语音活动检测（Silero VAD），不生成转录文本；检测模型与运行环境均已打包，不需要下载或安装依赖。

## 开始使用

1. 双击 `LectureInk.exe`，无需安装 Python、Qt 或其他依赖。需要 Windows 10（1903 或更新版本）／Windows 11，64 位，联网。
2. 在“连接设置”填写自己的硅基流动 API Key，可勾选“在本机加密记住密钥”。分享包不含任何密钥。
3. 填写课程主题，按需补充术语中英对照或简短背景。它们会随每次请求发送给模型。
4. 在“声音来源”选择“系统声音 · 网课播放”或“麦克风 · 现场收音”，再在下方选择设备。点“开始听课”，播放课程或对着所选麦克风讲话。
5. 声音条变化代表已收到音频。系统声音模式应选择播放器正在使用的耳机或扬声器；麦克风模式应选择实际使用的内置、USB 或耳麦输入设备。新接入设备后点“刷新设备”。
6. 文稿持续向下累积。可切换“中文／双语”，选择复制；点“缩小字号／放大字号”同时调整原文和译文，中间数字显示译文字号。取消“跟随最新内容”后可回看前文。
7. 结束时先暂停课程，再点“结束并处理剩余音频”，等待剩余翻译完成。通过“导出文稿”保存 TXT、Markdown 或 HTML。

声音来源及各自选中的设备会记住。收音期间不能切换设备，请先结束本次听课。鼠标悬停可查看设备完整名称，展开下拉列表也有更宽的空间。缩小窗口时，课程设置可独立滚动，开始／结束按钮固定可见。

麦克风没有声音时，检查设备连接、麦克风静音、输入音量，以及 Windows“隐私和安全性 → 麦克风”中的桌面应用访问权限。程序每次只采集一种来源，不同时混合系统声音与麦克风。

## 翻译与延迟

- 新版默认“快速预览 · 8 秒”，保留约 5 秒前文和 2 秒后文；首次预览通常需要先积累约 10 秒音频，再加模型处理时间。已有设置会保留，可手动切换到快速预览。
- 还可选择 12 秒均衡、18 秒更多语境或 26 秒长语境。程序在附近停顿处切段，停顿不等于句子一定完整。
- 音频仍按段提交；结果采用流式接收。下方固定的“即时预览”区域只显示已经组成原文／译文配对的内容，不把每个网络数据块变成新行。
- 等待后文的末句留在预览中，结合后文校对后进入上方文稿。原文／译文在同一段内随窗口宽度自动折行；同一句不会因为多次收到结果而反复新增段落。
- 每次带入最近最多 16 条原文与译文。尚未确认的末句只带原文草稿，不用其临时中文措辞约束后续翻译。未完句可保留最多约 12 秒前文音频。
- 未完成的句子显示“待续”。后文到来后会尝试补全。必要时针对句子边界再听一次音频。
- 收音期间，最后一句会留在预览区等待下一批重叠音频校对；结束时显示所有剩余内容。处理中导出时，尚未确认的内容会标注“待校对”或“待续”。
- “需核对”表示模型报告听不清，或者程序未能可靠拼接句子。这不是经过校准的置信度；没有标记也不代表一定正确。
- 模型可能听错口音、公式、专名或漏掉口头重复。专业内容仍应结合课件核对，不能承诺逐字准确。
- 模型返回的时间边界不能用于精确逐句音频剪辑，因此本版不输出看似精确的逐句时间戳。

## 没有人说话时

- 使用人声检测拦截静音、底噪和其他非语音声音，未检测到人声的片段不发送翻译请求。
- 判断范围是这次新增的音频，前一段重叠的人声不能单独触发下一段静音的翻译。
- 提示词再次要求无可辨人声时返回空结果；空结果不显示无关文字，也不会因为背景声有音量就报错并停止。
- 检测并非百分之百准确，极短、很轻或严重被噪声覆盖的讲话可能漏检，电视人声、歌词等也可能通过检测。不要将麦克风离讲话者太远。
- 原始音频仍完整保存在本机，过滤区间及原因记录在 `session.json` 的 `filtered_spans` 中，便于排查；检测加载失败会停止处理并保留音频，不会悄悄跳过检测。

## 保存与恢复

- 会话保存在 `%LOCALAPPDATA%\LectureInk\sessions`，每次课程一个目录。
- `session.json` 保存双语文稿、课程主题、声音来源、处理位置和请求耗时，`audio.pcm` 保存所选来源的音频，用于失败后重试与异常退出恢复。48 kHz 单声道约占 346 MB/小时。
- 点“打开文稿”，选择该目录中的 `session.json`，即可重看文稿。若还有未处理音频，可点“重试未完成的音频”。
- 网络／接口错误后会停止继续收音并保留已有音频。请暂停课程，恢复网络或更换有效密钥后重试。
- 处理积压超过 150 秒时会停止收音，避免无限积压；已采集的内容不丢弃。
- 若不再需要某次音频，可在关闭程序后删除对应会话目录；不要只删除还需要恢复的 `audio.pcm`。

## 密钥与分享

- 勾选记住密钥后，使用 Windows DPAPI 加密，只有当前 Windows 用户能解密；密钥不写入文稿、日志或源码。
- 为方便临时使用，也支持程序同目录的 `apikey.txt`，或 `SILICONFLOW_API_KEY` 环境变量。
- 分享时只发 EXE 或干净分享包，接收者填写自己的密钥。不需要安装软件依赖，但仍需要可用的硅基流动账号额度。
- 系统声音模式会采集所选输出设备中的其他声音；麦克风模式会采集麦克风周围的声音。

## 源码与构建

源码在 `src/`，测试在 `tests/`。开发环境依赖见 `requirements.txt`，运行 `build.ps1` 可测试并重新打包。最终用户直接运行已打包 EXE。

官方接口说明：https://docs.siliconflow.cn/docs/userguide/capabilities/multimodal-vision
